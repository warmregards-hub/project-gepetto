export default {
    // Tailwind v4 uses index.css for most config, but keeping this file as per specification
    theme: {
        extend: {
            colors: {
                accent: {
                    DEFAULT: '#E8825A',
                    hover: '#d76e46'
                }
            }
        }
    }
}
